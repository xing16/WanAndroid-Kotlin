<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.xing.wanandroid">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:name=".app.MainApp"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:networkSecurityConfig="@xml/network_security_config"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:usesCleartextTraffic="true">
        <activity android:name=".main.MainActivity">

        </activity>
        <activity
                android:name=".activity.SplashActivity"
                android:theme="@style/FullScreenStyle">

        </activity>
        <activity android:name=".web.WebViewActivity"></activity>
        <activity android:name=".search.SearchActivity"></activity>
        <activity android:name=".meizi.MeiziActivity"></activity>
        <activity android:name=".image.ImageBrowseActivity"></activity>
        <activity android:name=".user.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".user.activity.RegisterActivity"></activity>
        <activity android:name=".system.SystemArticleActivity"></activity>
    </application>

</manifest>